{
  "name": "chessbot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "esbuild --watch src/* --outdir=dist --bundle",
    "backend": "esbuild --watch --bundle --platform=node program.ts --outfile=program.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.1.7",
    "chrome-types": "^0.1.194",
    "esbuild": "^0.17.19",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "node-native-messaging-host": "^1.0.5"
  }
}
