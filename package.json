{
  "name": "chessbot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "esbuild --watch src/client/* --outdir=dist/client --bundle",
    "backend": "esbuild --watch src/backend/* --bundle --platform=node --target=node12 --outdir=dist/backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.1.7",
    "chrome-types": "^0.1.194",
    "esbuild": "^0.17.19",
    "typescript": "^5.0.4"
  }
}
